# Daily briefings

One file per weekday, written automatically by the "Daily G10 CB briefing"
workflow (`.github/workflows/briefing.yml`). Each briefing is also posted
as a comment on the rolling issue labeled `cb-briefings` — subscribe to
that issue to get them by email.
